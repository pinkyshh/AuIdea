<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/homeActivity">

    <activity
        android:id="@+id/homeActivity"
        android:name="lipika.androidapp.gridlayoutadvisor.HomeActivity"
        android:label="HomeActivity"
        tools:layout="@layout/activity_home"/>

    <activity
        android:id="@+id/testActivity"
        android:name="lipika.androidapp.gridlayoutadvisor.SecondActivity"
        android:label="Advisors"
        tools:layout="@layout/advisor_grid" />
</navigation>